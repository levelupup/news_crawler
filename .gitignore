venv/
__pycache__/
*.pyc
*.pyo
.env
news_history_all.html
googlesheet.py
CLAUDE.md
.claude/settings.local.json

# Full local history — never pushed to GitHub. Only data/recent.csv (the 2-day
# slice) is tracked; the old per-domain data/*.csv files are removed from git.
archive/
data/*.csv
!data/recent.csv

# Article body content scraped by content_scraper.py — local only, can be tens of MB.
# Never pushed (would bloat repo + slow the 30-min push cycle).
data/content/
data/ofweek_content.json
CONTENT_SCRAPER_SPEC.md
run_content.sh

# Crawler/analysis code stays LOCAL only (Actions removed; repo publishes outputs only).
*.py

# Machine-specific operational scripts (live only on gx10-4336; contain local paths)
push_to_github.sh
setup-root.sh
setup-git-auth.sh
backfill_archive.py
run_analyze.sh

# All local shell scripts + deps manifest — operational, not published output
*.sh
requirements.txt

# Handoff/setup doc for local Windows bridge — not relevant to published repo
WSJ_WINDOWS_HANDOFF.md

# stale output of the deprecated news_vectorizing.py — not regenerated
today_clustered.csv

# Local LLM prompt/persona doc — never published
DIGITIMES reader persona
push_failures.log

# india_search.py 的搜尋聚合 staging（india_electronics.py 讀取）— 本地交換檔，
# 內容已由 archive/recent.csv 管線正式收錄，不需獨立發布
data/india_electronics.jsonl

# Login credentials + Playwright session state for paywalled sources — NEVER push
secrets/

# Local cache/state files regenerated by the crawler scripts — not published output
analysis_7d_cache.json
data/sina_bulletin_state.json
